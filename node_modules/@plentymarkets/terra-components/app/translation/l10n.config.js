"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var angular_l10n_1 = require("angular-l10n");
exports.l10nConfig = getL10nConfig();
function getL10nConfig() {
    var langInLocalStorage = localStorage.getItem('plentymarkets_lang_');
    var lang = null;
    if (langInLocalStorage !== null) {
        lang = langInLocalStorage;
    }
    else {
        lang = navigator.language.slice(0, 2).toLocaleLowerCase();
        if (lang !== 'de' && lang !== 'en') {
            lang = 'en';
        }
        localStorage.setItem('plentymarkets_lang_', lang);
    }
    var prefix = null;
    // Definitions for i18n
    if (process.env.ENV === 'production') {
        prefix = 'app/assets/lang/locale-';
    }
    else {
        prefix = 'src/app/assets/lang/locale-';
    }
    return {
        locale: {
            languages: [
                {
                    code: 'en',
                    dir: 'ltr'
                },
                {
                    code: 'de',
                    dir: 'ltr'
                }
            ],
            language: lang,
            storage: angular_l10n_1.StorageStrategy.Cookie
        },
        translation: {
            providers: [
                {
                    type: angular_l10n_1.ProviderType.Static,
                    prefix: prefix
                }
            ],
            caching: true,
            composedKeySeparator: '.',
            i18nPlural: false
        }
    };
}

//# sourceMappingURL=l10n.config.js.map
