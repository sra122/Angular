import { ElementRef, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import * as Interact from 'interactjs';
import { DropEvent } from './dropEvent.interface';
export declare type AcceptFn = (args: {
    interactEvent: Interact.InteractEvent;
    event: MouseEvent;
    isDropable: boolean;
    dropzone: Interact.Interactable;
    dropzoneElement: HTMLElement;
    draggable: Interact.Interactable;
    draggableElement: HTMLElement;
    dragData: any;
}) => boolean;
export declare class TerraDropzoneDirective implements OnChanges {
    private el;
    accept: AcceptFn | string;
    overlap: 'pointer' | 'center' | number;
    disabled: boolean;
    onDropActivate: EventEmitter<DropEvent>;
    onDropDeactivate: EventEmitter<DropEvent>;
    onDragEnter: EventEmitter<DropEvent>;
    onDragLeave: EventEmitter<DropEvent>;
    onDropMove: EventEmitter<DropEvent>;
    onDrop: EventEmitter<DropEvent>;
    private interactable;
    constructor(el: ElementRef);
    ngOnChanges(changes: SimpleChanges): void;
    private init();
}
