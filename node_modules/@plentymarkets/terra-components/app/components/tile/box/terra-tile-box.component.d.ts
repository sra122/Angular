import { TerraButtonInterface } from '../../buttons/button/data/terra-button.interface';
import { TerraTileBoxColor } from './data/terra-tile-box-color';
/**
 * @author mkunze
 * @deprecated use `terra-card` instead.
 */
export declare class TerraTileBoxComponent {
    inputTitle: string;
    inputSubTitle: string;
    inputText: string;
    inputImagePath: string;
    inputIsSelected: boolean;
    inputIsDragging: boolean;
    inputIsDropTarget: boolean;
    inputIsHover: boolean;
    inputColor: TerraTileBoxColor;
    inputIsLineView: boolean;
    inputButtonList: Array<TerraButtonInterface>;
    private stopPropagation(event);
    private setClassesToTileBoxItem();
}
