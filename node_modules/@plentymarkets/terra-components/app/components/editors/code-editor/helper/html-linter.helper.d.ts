import { HtmlLinterMessageInterface } from './html-linter-message.interface';
export declare class HtmlLinter {
    private rules;
    constructor(rules: Array<string>);
    verify(input: string): Array<HtmlLinterMessageInterface>;
}
