import { ElementRef, OnInit } from '@angular/core';
import { TranslationService } from 'angular-l10n';
import { TerraBaseEditorComponent } from '../base-editor/terra-base-editor.component';
import { TerraOverlayComponent } from '../../layouts/overlay/terra-overlay.component';
import { TerraButtonInterface } from '../../../../';
export declare class TerraCodeEditorComponent extends TerraBaseEditorComponent implements OnInit {
    protected translation: TranslationService;
    protected myElement: ElementRef;
    showCodeView: boolean;
    editorContent: string;
    rawContent: string;
    overlay: TerraOverlayComponent;
    protected viewConfirmation: {
        primaryButton: TerraButtonInterface;
        secondaryButton: TerraButtonInterface;
    };
    protected isValidMarkup: boolean;
    protected invalidMarkupHint: string;
    private isInitialized;
    private linter;
    constructor(translation: TranslationService, myElement: ElementRef);
    writeValue(value: string): void;
    ngOnInit(): void;
    protected emitChanges(isEditorContent?: boolean): void;
    protected closeCodeView(forceClose?: boolean): void;
    private checkCodeFormat();
    private validateMarkup();
    private safeHtml(input);
}
