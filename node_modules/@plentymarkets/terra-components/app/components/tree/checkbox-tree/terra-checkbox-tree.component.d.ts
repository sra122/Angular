import { EventEmitter, OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { TerraBaseTreeComponent } from '../base/terra-base-tree.component';
import { TerraCheckboxLeafInterface } from '../leaf/terra-checkbox-leaf.interface';
export declare class TerraCheckboxTreeComponent extends TerraBaseTreeComponent implements OnInit, OnChanges {
    /**
     * @description current level leaf list
     */
    inputLeafList: Array<TerraCheckboxLeafInterface>;
    /**
     * @description leafs one level higher than current leaf
     */
    inputParentLeafList: Array<TerraCheckboxLeafInterface>;
    /**
     * @description complete leaf list for better and faster searching
     */
    inputCompleteLeafList: Array<TerraCheckboxLeafInterface>;
    /**
     * @description get the current selected leaf list
     */
    valueChange: EventEmitter<TerraCheckboxLeafInterface>;
    selectedLeafList: Array<TerraCheckboxLeafInterface>;
    /**
     * @description event which is triggered when any checkbox is clicked
     * @param event
     * @param leaf
     */
    private onCheckboxValueChange(event, leaf);
    private resetIndeterminateLeafState(leaf);
    /**
     * @description
     * @param leaf
     */
    private recursiveAddLeafToList(leaf);
    /**
     * @description
     * @param leaf
     */
    private recursiveUpdateChildLeafs(leaf);
    private recursiveUpdateParentLeafs(leaf);
    private updateStateValuesOfLeaf(leaf, isIndeterminate, checkboxChecked);
    private getParentLeafState(leaf);
    private appendParentsToLeafList(leafList);
    private recursiveAppendParentToSubLeafs(leaf);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private recursiveSetIndeterminateToParent(leaf);
}
