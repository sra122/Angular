import { OnInit } from '@angular/core';
import { TerraLeafInterface } from '../leaf/terra-leaf.interface';
/**
 * @author mkunze
 */
export declare class TerraBaseTreeComponent implements OnInit {
    /**
     * current level leaf list
     */
    inputLeafList: Array<TerraLeafInterface>;
    /**
     * leafs one level higher than current leaf
     */
    inputParentLeafList: Array<TerraLeafInterface>;
    /**
     * complete leaf list for better and faster searching
     */
    inputCompleteLeafList: Array<TerraLeafInterface>;
    ngOnInit(): void;
    private onLeafClick(clickedLeaf);
    private recursiveLeafListInactive(list);
    private toggleOpen(clickedLeaf);
    private onArrowClick(clickedLeaf);
    private recursiveSearchActiveLeaf(leafListToSearch);
    getSelectedLeaf(): TerraLeafInterface;
    private checkIfArrowNeeded(clickedLeaf);
}
