"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var terra_info_box_component_1 = require("./terra-info-box.component");
describe('Component: TerraInfoBoxComponent', function () {
    var component = new terra_info_box_component_1.TerraInfoBoxComponent();
    var tagList = [];
    tagList.push({
        name: 'test1'
    }, {
        name: 'test2'
    });
    afterEach(function () {
        component.inputTagList = [];
    });
    afterEach(function () {
        component.inputId = null;
    });
    it('should create an instance', function () {
        expect(component).toBeTruthy();
    });
    it('should getter \'hasFooter\' return true if \'inputTagList\' has entries', function () {
        component.inputTagList = tagList;
        expect(component.hasFooter).toBe(true);
    });
    it('should getter \'hasFooter\' return true if \'inputId\' has a value', function () {
        component.inputId = 5;
        expect(component.hasFooter).toBe(true);
        expect(component.inputId).toEqual(jasmine.any(Number));
        expect(component.inputId).toBeGreaterThan(0);
    });
    it('should getter \'hasFooter\' return false if \'inputTagList\' or \'inputId\' are null', function () {
        component.inputTagList = null;
        component.inputId = null;
        expect(component.hasFooter).toBe(false);
    });
    it('should getter \'hasFooter\' return false if \'inputTagList\' is empty', function () {
        component.inputTagList = [];
        expect(component.hasFooter).toBe(false);
    });
});

//# sourceMappingURL=terra-info-box.component.spec.js.map
