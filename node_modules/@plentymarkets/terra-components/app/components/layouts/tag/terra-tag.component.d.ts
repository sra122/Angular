import { EventEmitter } from '@angular/core';
import { TerraTagNameInterface } from './data/terra-tag-name.interface';
export declare class TerraTagComponent {
    lang: string;
    name: string;
    inputBadge: string;
    inputIsTagged: boolean;
    inputIsTaggable: boolean;
    inputCustomClass: string;
    inputColor: string;
    tagId: number;
    isClosable: boolean;
    names: Array<TerraTagNameInterface>;
    onCloseTag: EventEmitter<number>;
    constructor();
    protected close(): void;
    protected getName(): string;
    private getTranslatedName();
    /**
     * Get the background color.
     * @returns {string}
     * @see inputColor
     */
    private getBgColor();
    /**
     * Get the foreground color.
     */
    private getColor();
}
