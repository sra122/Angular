import { AfterContentChecked, ElementRef } from '@angular/core';
export declare class TerraCardComponent implements AfterContentChecked {
    viewChildHeader: ElementRef;
    viewChildFooter: ElementRef;
    inputImagePath: string;
    inputPlaceholderIcon: string;
    inputIsSelected: boolean;
    private showHeader;
    private showFooter;
    constructor();
    ngAfterContentChecked(): void;
}
