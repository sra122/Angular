import { OnDestroy, OnInit } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';
import { TerraTwoColumnsContainerComponent } from './terra-two-columns-container.component';
import { Subscription } from 'rxjs/Subscription';
export declare class TerraTwoColumnsContainerDirective implements OnInit, OnDestroy {
    private route;
    private router;
    private twoColComponent;
    subscription: Subscription;
    private basePath;
    constructor(route: ActivatedRoute, router: Router, twoColComponent: TerraTwoColumnsContainerComponent);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private setColumnHidden(column);
}
