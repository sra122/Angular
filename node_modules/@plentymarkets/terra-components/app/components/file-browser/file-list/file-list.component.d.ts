import { AfterViewInit, ChangeDetectorRef, OnChanges, OnDestroy, OnInit, SimpleChanges } from '@angular/core';
import { TerraBaseStorageService } from '../terra-base-storage.interface';
import { TerraFileBrowserComponent } from '../terra-file-browser.component';
import { TerraFileBrowserService } from '../terra-file-browser.service';
import { TranslationService } from 'angular-l10n';
import { TerraUploadProgress } from '../model/terra-upload-progress';
import { TerraStorageObjectList } from '../model/terra-storage-object-list';
import { TerraStorageObject } from '../model/terra-storage-object';
import { TerraSimpleTableRowInterface } from '../../tables/simple/row/terra-simple-table-row.interface';
import { TerraSimpleTableHeaderCellInterface } from '../../tables/simple/cell/terra-simple-table-header-cell.interface';
export declare class TerraFileListComponent implements OnInit, AfterViewInit, OnChanges, OnDestroy {
    private changeDetector;
    private fileBrowserService;
    private translationService;
    protected parentFileBrowser: TerraFileBrowserComponent;
    inputStorageServices: Array<TerraBaseStorageService>;
    protected translationPrefix: string;
    protected isDragActive: boolean;
    protected progress: TerraUploadProgress;
    protected storageList: TerraStorageObjectList;
    protected showNewDirectoryPrompt: boolean;
    protected objectsToDelete: Array<TerraStorageObject>;
    protected selectedStorageObjects: Array<TerraStorageObject>;
    protected fileTableHeaderList: Array<TerraSimpleTableHeaderCellInterface>;
    protected fileTableRowList: Array<TerraSimpleTableRowInterface<TerraStorageObject>>;
    private _activeStorageService;
    activeStorageService: TerraBaseStorageService;
    private fileDropzoneElement;
    private fileTableComponent;
    private dragSubscription;
    private storageSubscription;
    private progressSubscription;
    private _currentStorageRoot;
    private imagePreviewTimeout;
    private imagePreviewObject;
    currentStorageRoot: TerraStorageObject;
    readonly parentStorageObjects: Array<TerraStorageObject>;
    private _newDirectoryName;
    newDirectoryName: string;
    protected readonly _deleteCount: number;
    private datePipe;
    private defaultLocale;
    constructor(changeDetector: ChangeDetectorRef, fileBrowserService: TerraFileBrowserService, translationService: TranslationService, parentFileBrowser: TerraFileBrowserComponent);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    protected createDirectory(): void;
    protected deleteObjects(): void;
    private renderFileList();
    private fillTableRowList();
    private createTableRow(storageObject);
    private addClipboardButton(storageObject, cellList);
    private addDownloadButton(storageObject, cellList);
    private addDeleteButton(storageObject, cellList);
    private createHeaderListDependingOnAccessLevel();
    private createPublicHeaderList();
    private createPrivateHeaderList();
    private isAllowed(filename);
    protected onActiveRowChange(row: TerraSimpleTableRowInterface<TerraStorageObject>): void;
    onSelectionChange(rows: Array<TerraSimpleTableRowInterface<TerraStorageObject>>): void;
    onFileSelect(event: Event): void;
    onFileDrop(event: DragEvent): void;
    private uploadFiles(fileList);
}
