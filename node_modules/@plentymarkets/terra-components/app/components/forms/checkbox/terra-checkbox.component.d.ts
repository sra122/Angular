import { EventEmitter } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
export declare class TerraCheckboxComponent implements ControlValueAccessor {
    /**
     * @description If true, the check box will be disabled. Default false.
     */
    inputIsDisabled: boolean;
    /**
     * @description Set the caption.
     */
    inputCaption: string;
    /**
     * @description Set an icon (e.g. icon-save).
     */
    inputIcon: string;
    /**
     * @description Set the id.
     * @deprecated inputId will be removed in next major release.
     */
    inputId: string;
    valueChange: EventEmitter<boolean>;
    isValid: boolean;
    /**
     * @description a unique string identifier for the specific input instance.
     */
    protected id: string;
    private innerValue;
    private _isIndeterminate;
    constructor();
    value: boolean;
    onChange(event: boolean): void;
    writeValue(value: boolean): void;
    registerOnChange(fn: (_: any) => void): void;
    registerOnTouched(fn: () => void): void;
    isIndeterminate: boolean;
    private onTouchedCallback;
    private onChangeCallback;
}
