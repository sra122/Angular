import { Type } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { TerraFormScope } from './model/terra-form-scope.data';
import { TerraFormFieldInterface } from './model/terra-form-field.interface';
export declare class TerraFormComponent implements ControlValueAccessor {
    inputFormFields: {
        [key: string]: TerraFormFieldInterface;
    };
    inputControlTypeMap: any;
    inputIsDisabled: boolean;
    protected scope: TerraFormScope;
    protected values: any;
    protected controlTypeMap: {
        [key: string]: Type<any>;
    };
    private formFields;
    private parseFormField(field);
    writeValue(values: any): void;
    private onChangeCallback;
    registerOnChange(callback: any): void;
    private onTouchedCallback;
    registerOnTouched(callback: any): void;
    protected onValueChanged(key: string, value: any): void;
}
