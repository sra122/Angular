import { EventEmitter, OnInit, Type } from '@angular/core';
import { TerraFormFieldInterface } from '../model/terra-form-field.interface';
import { TerraFormScope } from '../model/terra-form-scope.data';
export declare class TerraFormEntryListComponent implements OnInit {
    private static itemCount;
    inputFormField: TerraFormFieldInterface;
    inputFormFieldKey: string;
    inputFormValue: Array<any>;
    inputListRange: boolean | string;
    inputScope: TerraFormScope;
    inputControlTypeMap: {
        [key: string]: Type<any>;
    };
    inputIsDisabled: boolean;
    outputFormValueChanged: EventEmitter<any>;
    protected min: number;
    protected max: number;
    private value;
    private itemScopes;
    ngOnInit(): void;
    protected readonly canAddElement: boolean;
    protected addElement(): void;
    protected readonly canRemoveElement: boolean;
    protected removeElement(key: number): void;
    protected fillRange(): void;
    protected onElementValueChanged(key: number, value: any): void;
    protected trackByFn(index: number, item: {
        key: number;
        value: any;
    }): number;
    private emitValue();
    private compareValues(values);
    private createChildScopeData(value);
}
