import { OnChanges } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { IMyDateModel, IMyOptions, MyDatePicker } from 'mydatepicker';
/**
 * @author mfrank
 */
export declare class TerraDatePickerComponent implements OnChanges, ControlValueAccessor {
    /**
     * @description Set the label.
     */
    inputName: string;
    /**
     * @description If true, a * indicates that the value is required. Default false.
     */
    inputIsRequired: boolean;
    /**
     * @description If false, the input will appear with a red border to indicate that the entered value is not valid. Default true.
     */
    inputIsValid: boolean;
    /**
     * @description If true, the input will be disabled. Default false.
     */
    inputIsDisabled: boolean;
    /**
     * @description If true, the calendar will be opened on top. Default false.
     */
    inputOpenCalendarTop: boolean;
    /**
     * @description Set the date format. Default 'dd.mm.yyyy'.
     */
    inputDisplayDateFormat: string;
    viewChildMyDatePicker: MyDatePicker;
    /**
     * @description a unique string identifier for the specific input instance.
     */
    protected currentLocale: string;
    protected id: string;
    protected datePickerOptions: IMyOptions;
    private _value;
    constructor();
    ngOnChanges(): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    writeValue(value: any): void;
    value: IMyDateModel;
    clearDate(): void;
    private updateDatePickerOptions();
    private onTouchedCallback;
    private onChangeCallback;
}
