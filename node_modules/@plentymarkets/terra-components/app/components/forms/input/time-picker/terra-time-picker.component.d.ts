/**
 * @author twieder
 */
import { OnInit } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { TerraSelectBoxValueInterface } from '../../select-box/data/terra-select-box.interface';
export declare class TerraTimePickerComponent implements OnInit, ControlValueAccessor {
    /**
     * @description If true, the input will be disabled. Default false.
     * */
    inputIsDisabled: boolean;
    valuesHours: Array<TerraSelectBoxValueInterface>;
    valuesMinutes: Array<TerraSelectBoxValueInterface>;
    private value;
    constructor();
    ngOnInit(): void;
    createTimeValues(): void;
    private onTouchedCallback;
    private onChangeCallback;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    writeValue(value: Date): void;
    protected minutes: number;
    protected hours: number;
}
