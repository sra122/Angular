import { TerraFormFieldBase } from '../data/terra-form-field-base';
import { FormBuilder, FormGroup } from '@angular/forms';
/**
 * @author mfrank
 */
export declare class TerraFormFieldControlService {
    private formBuilder;
    dynamicFormGroup: FormGroup;
    defaultValues: {
        [key: string]: string | number | boolean;
    };
    translationMapping: {
        [key: string]: string;
    };
    private formFieldsToGroup;
    constructor(formBuilder: FormBuilder);
    /**
     * Creates a new FormGroup
     *
     * @param formFields
     */
    createFormGroup(formFields: Array<TerraFormFieldBase<any>>): void;
    /**
     * Resets the form to default values
     */
    resetForm(): void;
    /**
     * Resets the form to default values
     */
    updateDefaultValues(values: any): void;
    private initFormGroupHelper(formFields, toGroup, isDisabled?);
    private generateValidators(formField);
}
