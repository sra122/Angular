import { TerraFormFieldBase } from '../data/terra-form-field-base';
/**
 * @author mfrank
 *
 */
export declare class TerraJsonToFormFieldService {
    static generateFormFields(formFieldsJSON: {
        [key: string]: any;
    }): Array<TerraFormFieldBase<any>>;
    private static createFormField(formFieldKey, formFieldData);
    private static createContainerFormField(formFieldKey, formFieldData, containerType);
}
