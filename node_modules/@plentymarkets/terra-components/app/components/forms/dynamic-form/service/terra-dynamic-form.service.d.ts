import { TerraBaseService } from '../../../../service/terra-base.service';
import { TerraLoadingSpinnerService } from '../../../loading-spinner/service/terra-loading-spinner.service';
import { Http } from '@angular/http';
import { Observable } from 'rxjs/Observable';
/**
 * @author mfrank
 */
export declare class TerraDynamicFormService extends TerraBaseService {
    private spinnerService;
    private baseHttp;
    constructor(spinnerService: TerraLoadingSpinnerService, baseHttp: Http);
    /**
     * @param data
     * @param url
     * @return {any}
     */
    create(data: any, url: string, params: any): Observable<any>;
    /**
     * @param data
     * @param url
     * @return {any}
     */
    update(data: any, url: string, params: any): Observable<any>;
    /**
     * @param data
     * @param url
     * @return {any}
     */
    delete(data: any, url: string): Observable<any>;
}
