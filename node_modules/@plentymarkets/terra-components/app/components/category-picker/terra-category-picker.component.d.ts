import { AfterContentChecked, OnInit } from '@angular/core';
import { TranslationService } from 'angular-l10n';
import { TerraCategoryPickerBaseService } from './service/terra-category-picker-base.service';
import { CategoryDataInterface } from './data/category-data.interface';
import { CategoryValueInterface } from './data/category-value.interface';
import { TerraNestedDataPickerComponent } from '../nested-data-picker/terra-nested-data-picker.component';
import { NestedDataTreeConfig } from '../nested-data-picker/config/nested-data-tree.config';
export declare class TerraCategoryPickerComponent extends TerraNestedDataPickerComponent implements OnInit, AfterContentChecked {
    translation: TranslationService;
    nestedTreeConfig: NestedDataTreeConfig<CategoryDataInterface>;
    /**
     * @description Service, that is used to request the category data from the server
     */
    inputCategoryService: TerraCategoryPickerBaseService;
    private completeCategory;
    private categoryName;
    private list;
    private isContainerCategorySelected;
    constructor(translation: TranslationService, nestedTreeConfig: NestedDataTreeConfig<CategoryDataInterface>);
    ngAfterContentChecked(): void;
    ngOnInit(): void;
    getCompleteCategoryObject(): CategoryValueInterface;
    writeValue(value: any): void;
    onSelectNode(): void;
    reset(): void;
    private updateCompleteCategory(category);
    private getCategoriesByParentId(parentId);
    private getCategories(parentId);
    addNodes(data: any, parentNodeId: number | string): void;
    private getCategoriesByParent(parentNode);
}
