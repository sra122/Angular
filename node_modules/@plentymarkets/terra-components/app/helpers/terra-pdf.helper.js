"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var TerraPdfHelper = /** @class */ (function () {
    function TerraPdfHelper() {
    }
    TerraPdfHelper.createPdfBlob = function (base64String) {
        var sliceSize = 512;
        var byteCharacters = atob(base64String);
        var byteArrays = [];
        for (var offset = 0; offset < byteCharacters.length; offset += sliceSize) {
            var slice = byteCharacters.slice(offset, offset + sliceSize);
            var byteNumbers = new Array(slice.length);
            for (var i = 0; i < slice.length; i++) {
                byteNumbers[i] = slice.charCodeAt(i);
            }
            var byteArray = new Uint8Array(byteNumbers);
            byteArrays.push(byteArray);
        }
        return new Blob(byteArrays, { type: 'application/pdf' });
    };
    return TerraPdfHelper;
}());
exports.TerraPdfHelper = TerraPdfHelper;

//# sourceMappingURL=terra-pdf.helper.js.map
