"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var UrlHelper = /** @class */ (function () {
    function UrlHelper() {
    }
    UrlHelper.removeLeadingSlash = function (url) {
        if (url.startsWith('/')) {
            // remove leading slash in url
            return url.slice(1, url.length);
        }
        return url;
    };
    UrlHelper.removeFragment = function (url) {
        if (url.includes('#')) {
            return url.slice(0, url.indexOf('#'));
        }
        return url;
    };
    UrlHelper.removeQueryParams = function (url) {
        if (url.includes('?')) {
            return url.includes('#') ? url.replace(url.slice(url.indexOf('?'), url.indexOf('#')), '') : url.slice(0, url.indexOf('?'));
        }
        return url;
    };
    UrlHelper.getCleanUrl = function (url) {
        return this.removeFragment(this.removeQueryParams(this.removeLeadingSlash(url)));
    };
    return UrlHelper;
}());
exports.UrlHelper = UrlHelper;

//# sourceMappingURL=url.helper.js.map
